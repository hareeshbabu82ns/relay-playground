type Counter {
  counter: Int
}

type Link {
  _id: String
  title: String
  url: String
}

type Mutation {
  incrimentCounter: Int
}

type Query {
  counter: Int
  data: [Counter]
  store: Store
}

type Store {
  links: [Link]
}
